<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet permission for Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FYP"
        tools:targetApi="31">

        <!-- Launcher Activity -->
        <activity
            android:name=".LauncherActivity"
            android:exported="true"
            android:theme="@style/Theme.FYP"> <!-- Optional: Use a theme without action bar for splash -->

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".auth.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.FYP" /> <!-- Optional: No action bar -->

        <!-- Sign Up Activity -->
        <activity
            android:name=".auth.SignUpActivity"
            android:exported="false"
            android:theme="@style/Theme.FYP" /> <!-- Optional: No action bar -->

        <activity
            android:name=".ui.ai.AiTaskActivity"
            android:exported="false"
            android:theme="@style/Theme.FYP" />

        <!-- Main Activity (No longer the launcher) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"> <!-- Keep exported true if other apps might launch it directly, otherwise false -->
        </activity>
    </application>
</manifest>